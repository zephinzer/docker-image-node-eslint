/**
 * JSDocs are required, so here's one.
 */
(function thatsProbablyUseful() {})();

/**
 * All that is defined shall be used.
 */
const usefulVariable = 1;
const usedVariable = usefulVariable;
thatsProbablyUseful(usedVariable);

/**
 * Spaces are needed for all keywords
 */
if (usefulVariable == 1) {
  for (let itBe = '(Y)'; itBe.length > 0; itBe.splice(0, 1, null)) {
    while (itBe.length > 0) {
      itBe.splice(0, 1, null);
    }
  }
}

// the line below this line is intentional
